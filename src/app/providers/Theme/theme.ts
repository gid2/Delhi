import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        background: {
            default: '#000000', // Черный фон по умолчанию
        },
        text: {
            primary: '#ffffff', // Белый цвет текста
        },
    },
});
